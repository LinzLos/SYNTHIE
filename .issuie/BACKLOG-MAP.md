# Backlog map — LinzLos/SYNTHIE

Generated by issuie from **today's SYNTHIE findings** (self-audit of the Surface B
analysis + repo review, 2026-06-14). **✅ 5 issues created (#1–#5) + pinned tracking
issue #6.**

> Context: the regression-case gap is **already closed** (cases 004/005 committed
> `5e7496f`, pushed), so it's not in this backlog. What remains are the **script +
> repo** improvements surfaced today. Most edit one shared file — the next version
> script — so they serialize into a single **v1.5 cut**.

## ⚠️ Scheduling reality: the script is one file
`scripts/versions/v1.5/SYNTHIE_v1.5.{xml,txt}` does not exist yet and **#1–#3 all
edit it**. They cannot run as parallel agents — they **serialize into one v1.5
branch**, then **#4 cuts the release**. #5 (a new case) is fully independent.

## Issues (#NN — GitHub)

| # | Type · Pri | Title | Touches | Source (today's finding) |
|---|---|---|---|---|
| #1 | feat · **P1** | Real plaintext `.txt` rendering (fix broken paste-fallback) | `scripts/versions/v1.5/*` *(shared)*, `README.md` | `.txt` is byte-for-byte the `.xml` → non-agent users paste raw XML |
| #2 | feat · **P1** | `source-fidelity` mode: raw vs derived input | `scripts/versions/v1.5/*` *(shared)* | No mode for second-order analysis; attribution unverifiable on derived input (today's own audit) |
| #3 | chore · P2 | Strip stale FLOWIE lending examples | `scripts/versions/v1.5/*` *(shared)* | `<differences>` still ships "relocation vs retail / banker vs ops" |
| #4 | chore · **P1** | Cut **v1.5** — bump manifest, CHANGELOG, version dir | `manifest.json`, `CHANGELOG.md`, `README.md`, `scripts/versions/v1.5/` | Release that ships #1–#3 |
| #5 | feat · P2 | Add case 006 — first **real-world** fixture from today's second-order audit | `cases/006-*`, `cases/README.md` | Today's Surface B meta-analysis is a real captured case; all current cases are designed traps |

**Tracking (pinned):** #6 — 📋 SYNTHIE v1.5 — roadmap & build order (start here)

## Waves

**Serialize-alone (shared script file — one at a time, on a v1.5 branch):**
#1 → #2 → #3

**Then:** #4 (release) — depends on #1–#3 merged.

**Parallel-safe (independent, any time):** #5 — touches only `cases/`.

## Dependency graph
```
#1 ─┐
#2 ─┼─▶ #4   (v1.5 release ships the three script changes)
#3 ─┘
#5           (independent — new regression case)
```

## Hand-offs (cross-owner)
None — single maintainer (Lindsay). All internal sequencing.

## Needs scoping
None.

## Roadmap read (what to actually prioritize)
1. **#1 + #2 are the P1 substance** — the broken plaintext path is a real user-facing
   bug; `source-fidelity` is the gap today's audit walked into. Both are v1.5.
2. **#4 bundles them into a clean v1.5 release** (version bump is cheap once #1–#3 land).
3. **#3** is low-risk polish — fold into the same v1.5 branch.
4. **#5** can run independent of the version work — turns today's real audit into the
   repo's first non-designed fixture (stronger than a trap, per cases/README's own note).

---

# Backlog map — post-v1.7 (2026-09-06)

v1.5 backlog closed (#1–#6). New wave from the 2026-09-05/06 sessions: the
operator is the last unbuilt sentence in the README's "Where it's going".

| # | Type · Pri | Title | Touches | Depends on |
|---|---|---|---|---|
| #7 | feat · **P1** | Build the SYNTHIE operator: subagent + sweep + blind-run mode | `.claude/agents/synthie.md`, `scripts/synthie-sweep.sh`, `operator/*`, `README.md`, `cases/README.md` | — |
| #8 | feat · P2 | Theme-drift detection between sweeps (`evidence-persistence` invariant, case 007) | `scripts/versions/v1.8/*`, `scripts/synthie-drift.sh`, `cases/007-*` | #7 |

Sequence: #7 → #8. Independent of both: the pending real-transcript blind
run of v1.7 (raw path, first real material), whose misses may claim case 007
first — renumber #8's case if so.
